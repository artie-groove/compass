extends ../templates/page-light.tpl.pug

block content
	.wrapper#content-tours
			.container.cards-grid
				.row
					.col-12
						h2 Визы
						p Выберите, в какую страну нужна виза
					each destination, i in data.visas
						.col-md-4.col-lg-3
							+destination-card(normalizeNumber(i+1), 'visas', destination, 'visa-info-china.html')

					